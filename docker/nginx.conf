events {}

http {
  server {
    listen 80;

    location / {
      proxy_pass http://mtranserver:8989;
      proxy_http_version 1.1;
      proxy_read_timeout 10m;
      proxy_send_timeout 10m;
      client_body_timeout 10m;
    }
  }
}
